# ALMA REST API 使用教學

本文件說明如何以 headless REST API 操作 ALMA，尤其是如何用 JSON 在執行期間建立角色。

本 adaptor 的語意依據依序是原始 ALMA 論文、專案 XSD 與原始核心程式。ALMA 的三層模型是短期 emotion、中期 mood、長期 personality；輸入先經 appraisal 形成 EEC，再由 emotion 影響 PAD mood。REST 不自行發明另一套情緒規則。

## REST adaptor 與 ALMA 核心的邊界

REST 功能實作在 `src/de/affect/rest/AlmaRestServer.java`，情緒、心情、人格、appraisal 與 decay 等 ALMA 原始運算核心不做修改。REST adaptor 只負責：

- 把 JSON 轉成 ALMA 原生的輸入文件與型別，再交給原始模型運算。
- 從 ALMA 的 character/group manager 讀取即時狀態並轉成 JSON。原始 `getCurrentMoodTendency()` 誤回傳 current mood，因此 adaptor 只讀取同一核心物件內真正的 tendency 欄位；未更改核心。
- 對原始核心會靜默丟棄、覆寫規則或留下未清 EEC 的已知情況做 fail-fast 驗證；不在 REST 內重寫情緒演算法。
- 在記憶體中關閉 Runtime Interaction Monitor，避免 REST 模式開啟 GUI；磁碟上的 `conf/*.aml` 不會被改寫。
- 在記憶體中建立動態角色與群組；服務關閉後資料即消失。

原始 ALMA 的 `DesktopHelper` 在沒有圖形環境時可能先印出一段已被其自身捕捉的 `java.awt.HeadlessException`。只要後續出現 `[alma-rest] listening` 且 `/health` 回傳 `status: ok`，REST 服務就是正常運作，也不會出現 GUI。

## 1. 啟動服務

第一次使用先編譯：

```bash
cd /mnt/d/ALMA
./build.sh fast
```

啟動 REST Server：

```bash
./run_rest.sh
```

預設設定如下：

- 位址：`http://localhost:8080`
- 全域計算設定：`conf/AffectComputationExample.aml`
- 初始角色設定：`conf/AffectDefinitionExample.aml`
- GUI：不啟動

指定其他 port：

```bash
./run_rest.sh --port 9090
```

指定其他 ALMA 設定：

```bash
./run_rest.sh \
  --comp conf/AffectComputationExample.aml \
  --def conf/AffectDefinitionExample.aml \
  --port 8080
```

確認服務：

```bash
curl http://localhost:8080/health
```

成功回應：

```json
{"status":"ok","alma_version":"3.0"}
```

## 2. 動態角色的生命週期

透過 `POST /characters` 建立的角色只存在 ALMA 程序的記憶體中：

- 建立後立即可以接收事件與查詢情感狀態。
- 不會修改 `conf/*.aml`。
- 不會自動儲存到資料庫。
- 關閉 `run_rest.sh`、程序異常結束或重新啟動服務後，動態角色會消失。
- 重新啟動後，只會重新載入 `--def` 指定的 AML 角色。

## 3. 建立角色

### Endpoint

```http
POST /characters
Content-Type: application/json
```

完整範例位於 `scripts/CreateCharacterExample.json`，可直接呼叫：

```bash
curl -i -X POST http://localhost:8080/characters \
  -H 'Content-Type: application/json' \
  --data-binary @scripts/CreateCharacterExample.json
```

成功時回傳 HTTP `201 Created`：

```json
{
  "created": true,
  "name": "Alice",
  "derived": false,
  "internal_affect_appraisal": false,
  "persistent": false
}
```

`persistent: false` 表示角色未持久化，服務停止後會消失。

### 完整 JSON

```json
{
  "name": "Alice",
  "personality": {
    "openness": 0.5,
    "conscientiousness": 0.3,
    "extraversion": 0.7,
    "agreeableness": 0.6,
    "neurotism": -0.2,
    "emotion_influence": 0.2,
    "derived": false
  },
  "mood": {
    "decay_time": 600000,
    "decay_period": 250,
    "neurotism_stability": false
  },
  "emotion": {
    "decay_time": 20000,
    "decay_period": 500,
    "decay_function": "linear",
    "baseline": 0.0
  },
  "internal_affect_appraisal": false,
  "appraisal": {
    "GoodEvent": {
      "desirability": 0.7
    },
    "GoodEventForGoodOther": {
      "agency": "other",
      "desirability": 0.6,
      "liking": 0.6
    },
    "GoodEventForBadOther": {
      "agency": "other",
      "desirability": 0.6,
      "liking": -0.4
    },
    "BadEvent": {
      "desirability": -0.7
    },
    "BadEventForGoodOther": {
      "agency": "other",
      "desirability": -0.6,
      "liking": 0.6
    },
    "BadEventForBadOther": {
      "agency": "other",
      "desirability": -0.6,
      "liking": -0.4
    },
    "GoodLikelyFutureEvent": {
      "desirability": 0.5,
      "likelihood": 0.5
    },
    "GoodUnlikelyFutureEvent": {
      "desirability": 0.5,
      "likelihood": -0.5
    },
    "BadLikelyFutureEvent": {
      "desirability": -0.5,
      "likelihood": 0.5
    },
    "BadUnlikelyFutureEvent": {
      "desirability": -0.5,
      "likelihood": -0.5
    },
    "EventConfirmed": {
      "realization": true
    },
    "EventDisconfirmed": {
      "realization": false
    },
    "GoodActSelf": {
      "praiseworthiness": 0.5,
      "agency": "self"
    },
    "GoodActOther": {
      "praiseworthiness": 0.5,
      "agency": "other"
    },
    "BadActSelf": {
      "praiseworthiness": -0.5,
      "agency": "self"
    },
    "BadActOther": {
      "praiseworthiness": -0.5,
      "agency": "other"
    },
    "NiceThing": {
      "appealingness": 0.5
    },
    "NastyThing": {
      "appealingness": -0.5
    }
  }
}
```

最上層的 `name`、`personality`、`mood`、`emotion`、`appraisal` 全部必填；`complex_appraisal` 與 `internal_affect_appraisal` 選填。欄位名稱區分大小寫，未知欄位會回 `400`，不會被靜默忽略。

## 4. `name`：角色名稱

```json
"name": "Alice"
```

規則：

- 必須是非空字串。
- 長度為 1～80 個字元。
- 目前允許英文字母、數字、空格、句點、底線與連字號。
- 不可包含保留分隔字串 ` - `；原始 group similarity summary 使用它分隔兩個角色名稱。
- 同一個 ALMA 程序內不可重複。
- 建立後，`/event`、`/pad`、`/affect/{name}` 都使用此名稱定位角色。

重複名稱會回傳 HTTP `409 Conflict`：

```json
{"error":"character already exists: Alice"}
```

## 5. `personality`：Big Five 人格

```json
"personality": {
  "openness": 0.5,
  "conscientiousness": 0.3,
  "extraversion": 0.7,
  "agreeableness": 0.6,
  "neurotism": -0.2,
  "emotion_influence": 0.2,
  "derived": false
}
```

五大人格值域都是 `-1.0～1.0`：

| JSON 欄位 | 意義 | 負值傾向 | 正值傾向 |
|---|---|---|---|
| `openness` | 開放性 | 保守、偏好熟悉事物 | 好奇、接受新經驗 |
| `conscientiousness` | 盡責性 | 隨性、較少規劃 | 自律、有組織、重視責任 |
| `extraversion` | 外向性 | 內向、低社交刺激需求 | 外向、活躍、尋求刺激 |
| `agreeableness` | 親和性 | 競爭、懷疑、強硬 | 合作、信任、同理 |
| `neurotism` | 神經質 | 情緒穩定 | 易焦慮、敏感、情緒波動較高 |

`emotion_influence` 值域為 `0.0～1.0`，控制人格對情緒強度的影響程度：

- `0.0`：人格不調整情緒強度。
- `0.2`：溫和影響，適合作為起始值。
- `1.0`：人格影響最大，角色差異會很明顯。

人格不會像情緒一樣隨時間衰減。它也會透過全域 Personality/PAD 關係決定角色的預設心情。

`derived` 是原始 ALMA XSD 與輸出中的人格來源註記，預設 `false`。核心只保存並輸出這個 metadata；它不會因為設成 `true` 就自動推導 Big Five，五個 trait 仍必須明確提供。

## 6. `mood`：中期心情設定

```json
"mood": {
  "decay_time": 600000,
  "decay_period": 250,
  "neurotism_stability": false
}
```

| 欄位 | 型別 | 說明 |
|---|---|---|
| `decay_time` | 正整數 | 心情回到人格預設心情所使用的總時間，單位毫秒 |
| `decay_period` | 正整數 | 執行一次心情更新的週期，單位毫秒 |
| `neurotism_stability` | boolean | 是否依神經質加入心情穩定性／隨機變動計算 |

驗證規則：

- 兩個時間都必須大於零。
- 必須是整數，不可傳字串或小數。
- `decay_period` 不可大於 `decay_time`。
- `neurotism_stability` 必須是 JSON boolean `true` 或 `false`，不可寫成字串 `"false"`。

範例 `600000 / 250` 表示以 250 毫秒為更新週期，整體以約 10 分鐘的尺度回歸預設心情。

週期越短，更新越頻繁，也會增加 CPU 與 Timer 工作量。通常不需要低於 100～250 毫秒。

## 7. `emotion`：短期情緒設定

```json
"emotion": {
  "decay_time": 20000,
  "decay_period": 500,
  "decay_function": "linear",
  "baseline": 0.0
}
```

| 欄位 | 型別／值域 | 說明 |
|---|---|---|
| `decay_time` | 正整數 | 情緒衰減總時間，單位毫秒 |
| `decay_period` | 正整數 | 每次情緒衰減計算的週期，單位毫秒 |
| `decay_function` | 字串 | 衰減曲線，只能是 `linear`、`exponential`、`hyperbolic` |
| `baseline` | `0.0～1.0` | 情緒基線／最低門檻相關參數 |

三種衰減曲線：

- `linear`：固定速度下降，最容易預測。
- `exponential`：前期下降較快，後期拖尾。
- `hyperbolic`：使用雙曲線形式，呈現不同於線性與指數的變化。

同樣地，`decay_period` 不可大於 `decay_time`。範例 `20000 / 500` 表示情緒約在 20 秒尺度衰減，每 500 毫秒更新一次。

## 8. `appraisal`：事件評估規則

`appraisal` 定義「這個角色如何解讀事件」，是 JSON 中最重要的行為差異設定。REST `/event` 收到的 `tag` 必須對應角色已定義的 appraisal 規則，才會產生預期情緒。

基本格式：

```json
"appraisal": {
  "規則名稱": {
    "評估變數": 數值
  }
}
```

`appraisal` 必須完整包含原始 ALMA XSD 定義的 18 個 Basic tag，不可缺少、不可增加自訂 tag。每個 tag 也只能具有表格列出的必要屬性，不可缺少或增加屬性。一般評估數值範圍是 `-1.0～1.0`：

- 越接近 `1.0`：越強烈的正向評估。
- 越接近 `-1.0`：越強烈的負向評估。
- 越接近 `0.0`：影響越弱。

### 支援的基本規則

| 規則名稱 | 必填屬性 | 通常值域／固定值 | 主要用途 |
|---|---|---|---|
| `GoodEvent` | `desirability` | `0.0～1.0` | 對自己有利的事件，常導向 Joy |
| `BadEvent` | `desirability` | `-1.0～0.0` | 對自己不利的事件，常導向 Distress |
| `GoodEventForGoodOther` | `agency`, `desirability`, `liking` | `other`、正、正 | 喜歡的人遇到好事，常導向 HappyFor |
| `GoodEventForBadOther` | `agency`, `desirability`, `liking` | `other`、正、負 | 不喜歡的人遇到好事，可能導向 Resentment |
| `BadEventForGoodOther` | `agency`, `desirability`, `liking` | `other`、負、正 | 喜歡的人遇到壞事，常導向 Pity |
| `BadEventForBadOther` | `agency`, `desirability`, `liking` | `other`、負、負 | 不喜歡的人遇到壞事，常導向 Gloating |
| `GoodLikelyFutureEvent` | `desirability`, `likelihood` | 正、正 | 預期好事可能發生，常導向 Hope |
| `GoodUnlikelyFutureEvent` | `desirability`, `likelihood` | 正、負 | 好事但不太可能發生 |
| `BadLikelyFutureEvent` | `desirability`, `likelihood` | 負、正 | 預期壞事可能發生，常導向 Fear |
| `BadUnlikelyFutureEvent` | `desirability`, `likelihood` | 負、負 | 壞事但不太可能發生 |
| `EventConfirmed` | `realization` | 固定 `true` | 先前預期事件確認發生 |
| `EventDisconfirmed` | `realization` | 固定 `false` | 先前預期事件確認未發生 |
| `GoodActSelf` | `agency`, `praiseworthiness` | `self`、正 | 自己做了值得讚許的事，常導向 Pride |
| `GoodActOther` | `agency`, `praiseworthiness` | `other`、正 | 他人做了值得讚許的事，常導向 Admiration |
| `BadActSelf` | `agency`, `praiseworthiness` | `self`、負 | 自己做了不好的事，常導向 Shame |
| `BadActOther` | `agency`, `praiseworthiness` | `other`、負 | 他人做了不好的事，常導向 Reproach |
| `NiceThing` | `appealingness` | 正 | 有吸引力的物件，常導向 Liking |
| `NastyThing` | `appealingness` | 負 | 令人厭惡的物件，常導向 Disliking |

`agency` 只能傳字串 `"self"` 或 `"other"`。`realization` 必須傳 JSON boolean，不能傳字串：

```json
"EventConfirmed": {
  "realization": true
}
```

其他 appraisal 屬性必須是 JSON number，不可用字串：

```json
"GoodEvent": {
  "desirability": 0.7
}
```

以下寫法錯誤：

```json
"GoodEvent": {
  "desirability": "0.7"
}
```

### 不知道數值時的處理

18 個 tag 都必須存在。如果某一類型對角色的影響未知，可以把該 tag 的數值屬性設成 `0.0`，表示最弱程度；`agency` 與 `realization` 仍必須使用表格指定的固定值。但在原始 rule-based Basic／Complex 路徑中，`0.0` 不等於省略：核心以「小於 0」判負向，因此零落在非負側，仍可能建立 baseline emotion、history 與 elicitor，並參與 compound。只有 `/eec` 的未使用數值欄位才明確以 `0` 表示沒有提供該 EEC。

## 9. JSON 驗證與錯誤回應

建立失敗時通常回傳 HTTP `400 Bad Request`：

```json
{"error":"missing required field: mood"}
```

常見原因：

- JSON 語法錯誤，例如少逗號、括號或雙引號。
- 缺少最上層必填物件。
- 把 number 或 boolean 寫成字串。
- 人格或 appraisal 數值超過允許範圍。
- decay 時間不是正整數。
- `decay_period` 大於 `decay_time`。
- 使用不支援的 `decay_function`。
- appraisal tag 或屬性不符合 ALMA 的 XML schema。
- appraisal 並未完整包含全部 18 個 Basic tag。
- tag 包含多餘或缺少的屬性。
- 正向屬性使用負值，或負向屬性使用正值。

JSON 可包含空白與換行，屬性順序沒有要求。`complex_appraisal`、群組的 `characters`，以及多人 `addressees`／`listeners` 使用陣列。

原始 XSD 對部分訊號的 `intensity`、`elicitor` 等欄位設為 optional 並由核心補預設值；REST 契約刻意要求呼叫端明確傳入，以免預設值掩蓋整合錯誤。這是 adaptor 的 fail-fast 政策，不代表 ALMA XSD 本身把這些欄位列為必填。

## 10. Complex Appraisal：讓 `/act` 等語意訊號可運作

18 個 Basic tag 定義角色收到 `/event` tag 時的固定評估；選填的 `complex_appraisal` 則把特定行為、情緒或心情訊號映射到一組 appraisal。若要充分使用 ALMA 的角色互動模型，建立角色時應加入這些規則。

```json
"complex_appraisal": [
  {
    "kind": "self_act",
    "signal": "Help",
    "appraisal": {
      "GoodActSelf": {
        "praiseworthiness": 0.8,
        "agency": "self"
      }
    }
  },
  {
    "kind": "direct_act",
    "signal": "Help",
    "performer": "Bob",
    "appraisal": {
      "GoodActOther": {
        "praiseworthiness": 0.8,
        "agency": "other"
      }
    }
  }
]
```

每筆規則必須有 `kind`、`signal`、`appraisal`。`direct_*` 與 `indirect_*` 另須且只能多出 `performer`；`self_*` 不可帶 `performer`。多餘欄位會回 `400`。支援的 `kind`：

| `kind` | 訊號欄位意義 | 使用情境 |
|---|---|---|
| `self_act` | 行為 `type` | 自己執行此行為 |
| `direct_act` | 行為 `type` | 指定 performer 對自己執行此行為 |
| `indirect_act` | 行為 `type` | 旁聽指定 performer 對別人執行此行為 |
| `self_emotion` | ALMA 情緒名稱 | 自己表達該情緒 |
| `indirect_emotion` | ALMA 情緒名稱 | 指定 performer 表達該情緒 |
| `self_mood` | ALMA 心情詞 | 自己表達該心情 |
| `indirect_mood` | ALMA 心情詞 | 指定 performer 表達該心情 |

其中 `appraisal` 可只放這項語意訊號需要的 18 種 Basic tag 子集，但每個 tag 內部欄位與值域仍須完全符合前表。原始核心只有 Event、Action、Object 各一個 pending EEC 槽，所以單筆 Complex 規則每一類最多一個 tag；例如同時放 `GoodEvent` 與 `BadEvent` 會被拒絕。REST 會依 XSD 要求重新排序後驗證。

原始 `AppraisalRules` 的真實儲存 key 只有 `(entity/performer, signal)`，不包含 `kind`。因此下列情況會在核心互相覆寫，REST 會在建立角色或群組時回 `400`，不假裝兩條規則能共存：

- 同一 performer 與 signal 的 `direct_act`、`indirect_act`。
- 完全重複的規則。
- 不同 act/emotion/mood kind 碰巧使用同一儲存 pair。
- self rule 的 signal 與同名 Basic tag 衝突。

完整可執行範例已放在 `scripts/CreateCharacterExample.json`。

### 原生 internal affect appraisal

角色最上層可選填：

```json
"internal_affect_appraisal": false
```

設為 `true` 會把原始 XSD 的 `Appraisal@internalAffectAppraisal` 原樣啟用。核心會每 500 ms 檢查 dominant emotion 是否高於角色 `EmotionSpecification@baseline` 的全域門檻，並檢查 mood 是否非 Neutral；符合時把它們轉成該角色自己的 EmotionDisplay／MoodDisplay，再套用相符的 `self_emotion`／`self_mood` 規則。這個 emotion 門檻不等同 `GET /affect` 中每個 emotion 各自以 `intensity > baseline` 算出的 `active`。沒有 matching self rule 時不會產生反思情緒。

這個選項不是一般 emotion decay 或 mood 推進的必要條件；那兩者無論此值為何都會運作。它是獨立的原始 self-appraisal timer，`POST /pause` 只暫停 decay 與 mood computation，並不停止這個 timer，因此只有確實需要反覆自我評估時才開啟。

因為這個核心 timer 不經 REST adaptor，建立時會額外檢查同一自動 emotion/mood family 的規則，拒絕可能跨次走入原始 Love/Hate 壞掉路徑的組合。若 internal 角色所屬群組有 matching `indirect_emotion`／`indirect_mood`，群組或全域 pause 會回 `409`；這是因為若 adaptor 不先拒絕，核心 timer 就可能繞過 adaptor 並重新啟動群組 timer，REST 無法誠實保證已暫停。

原始 `AffectManager` 會在群組尚未初始化時就啟動 character internal timer，大型 definition 有 500 ms 啟動競態。REST adaptor 會在載入副本中暫時延後 flag，等所有群組與空規則容器就緒後，再啟動同一個原始 `AffectAppraisalSimulation`；DocumentManager 中的原 flag 會恢復。這也讓 internal 角色可以安全屬於一個合法省略 `Appraisal` 的群組，而不必擅自替原檔補成 18 條規則。

`alma internal emotion appraisal` 與 `alma internal mood appraisal` 是核心 timer 的保留 elicitor；外部 REST 請求使用這兩個字串會回 `422`，避免外部 history 與下一次自動 appraisal 意外合成 Love/Hate。

## 11. 列出角色

```bash
curl http://localhost:8080/characters
```

回應：

```json
{"characters":["Anne","Bob","Alice"]}
```

其中 Anne、Bob 來自啟動時載入的 AML；Alice 是 REST 動態建立的記憶體角色。

## 12. 傳入 Basic Appraisal tag

```http
POST /appraisal
Content-Type: application/json
```

```bash
curl -X POST http://localhost:8080/appraisal \
  -H 'Content-Type: application/json' \
  -d '{
    "character": "Alice",
    "tag": "GoodEvent",
    "intensity": 0.8,
    "elicitor": "gift-20260814-001"
  }'
```

欄位說明：

| 欄位 | 必填 | 說明 |
|---|---|---|
| `character` | 是 | 接收 appraisal 訊號的角色名稱 |
| `tag` | 是 | appraisal 規則名稱，例如 `GoodEvent` |
| `intensity` | 是 | JSON number，範圍 `0.0～1.0`，表示本次事件強度 |
| `elicitor` | 是 | 非空事件 ID；同一事件的多個 tag 或預期與結果必須共用相同值 |

請求必須剛好包含這四個欄位，不能缺少或增加欄位。`tag` 必須精確符合 18 種 Basic Appraisal tag 之一，包含大小寫。

成功回應：

```json
{
  "accepted": true,
  "character": "Alice",
  "tag": "GoodEvent",
  "intensity": 0.8,
  "elicitor": "gift-20260814-001",
  "signal_kind": "event"
}
```

`/appraisal` 是正確名稱；舊的 `/event` 完全相同並保留相容性。18 種 tag 並不全是 event：4 個 Act tag 會建立 ALMA Action，2 個 Thing tag 會建立 Object，其餘建立 Event；回應的 `signal_kind` 會明確標示。

`processSignal` 內的 appraisal 與 emotion 產生是同步的，請求成功後可立即 `GET /affect/{name}`。之後 emotion decay 與 mood 推進才是計時器驅動，因此稍後查詢會看到強度與 mood 改變。

驗證失敗時：格式、tag 或 intensity 問題回傳 `400 Bad Request`；角色不存在回傳 `404 Not Found`；JSON body 超過 1 MiB 回傳 `413 Payload Too Large`；內部情緒處理失敗回傳 `500 Internal Server Error`。

## 13. 直接傳入 EEC 評估變數

`POST /eec` 適合外部系統已完成 appraisal、希望直接把原始 ALMA EEC 變數交給 OCC 情緒計算時使用：

```bash
curl -X POST http://localhost:8080/eec \
  -H 'Content-Type: application/json' \
  -d '{
    "character": "Alice",
    "desirability": 0.8,
    "praiseworthiness": 0.0,
    "appealingness": 0.0,
    "likelihood": 0.0,
    "realization": 0.0,
    "liking": 0.0,
    "agency": "self",
    "elicitor": "quest-42"
  }'
```

六個評估數值都必填且須為 JSON number `-1.0～1.0`；不知道的值填 `0.0`。`agency` 只能為 `self` 或 `other`，`elicitor` 為必填非空事件識別。請求必須剛好包含上述九個欄位。

原始 ALMA 核心把數值 `0` 視為「未提供該變數」，而且只定義下列 8 種有效組合。未列出的非零組合會回 `400`，避免核心依固定分支順序只採用一部分、悄悄忽略其他值。

| 有效非零欄位 | ALMA 評估類型 |
|---|---|
| `realization` | 已有 prospective emotion 的確認／否認；須沿用相同 `elicitor` |
| `desirability` | Event |
| `desirability + likelihood` | Prospective Event |
| `desirability + liking` | Event concerning another |
| `praiseworthiness` | Action；`agency` 決定 self/other |
| `appealingness` | Object |
| `desirability + praiseworthiness` | Event + Action compound |
| `praiseworthiness + appealingness` | Action + Object pair；只有不會進入下述壞掉 Love/Hate 路徑的組合可接受 |

注意：`/event` 是「指定已建好的 tag 規則，再乘上本次 intensity」；`/eec` 是「直接提供這次評估的六個 EEC 數值」。兩者都會進入 ALMA 原始情緒模型，但輸入抽象層次不同。

### ALMA 3.0 Love/Hate frozen-core 限制

原始 `EmotionEngine` 的 Love/Hate 合成實作有 null dereference：`agency: "other"` 時，同號的正向 `praiseworthiness + appealingness` 會嘗試 Love，兩者皆負會嘗試 Hate，原始核心都可能崩潰並留下未清 EEC。依「核心不修改」原則，REST 不重寫這段演算法，而是在進入核心前回 `422 Unprocessable Entity`。同一 `elicitor` 分次送入 `GoodActOther` + `NiceThing` 或 `BadActOther` + `NastyThing` 也會被 character 輸入預檢阻止。

若其他尚未預見的核心例外仍發生，adaptor 會清除角色與群組尚未完成的 EEC queue，再回 `500`，避免一次失敗讓後續每次 inference 都持續失敗。這只做失敗復原，不改變任何成功路徑的 ALMA 計算。

## 14. 直接傳入 PAD

除了 appraisal event，也能直接輸入 Pleasure、Arousal、Dominance：

```bash
curl -X POST http://localhost:8080/pad \
  -H 'Content-Type: application/json' \
  -d '{
    "character": "Alice",
    "pleasure": 0.6,
    "arousal": 0.4,
    "dominance": 0.2,
    "intensity": 0.8,
    "description": "external-affect-model"
  }'
```

`pleasure`、`arousal`、`dominance` 須為 JSON number `-1.0～1.0`，`intensity` 為 `0.0～1.0`，`description` 為非空來源描述。舊欄位 `elicitor` 暫時仍可作相容別名，但不可與 `description` 同時傳入。

- `pleasure`：正值較愉快，負值較不愉快。
- `arousal`：正值較激發，負值較平靜。
- `dominance`：正值較有掌控感，負值較受支配。

重要：原始 ALMA 將 PAD 輸入建立成 `Physical` emotion（最初用於 biosensor），再由 emotion-to-mood 機制逐步影響 mood。它不是「直接指定目前 mood」，也不能當作重啟後還原 mood snapshot 的 API。

## 15. 傳入角色行為

`POST /act` 讓 ALMA 同時依 performer、addressee、listener 的 Complex Appraisal 規則解讀一個行為：

```bash
curl -X POST http://localhost:8080/act \
  -H 'Content-Type: application/json' \
  -d '{
    "performer": "Bob",
    "addressee": "Alice",
    "listener": "Anne",
    "type": "Help",
    "intensity": 0.9,
    "elicitor": "help-001"
  }'
```

`performer` 必填且須存在。`addressee`、`listener` 均可省略；多人時改用字串陣列 `addressees`、`listeners`，同一角色欄位不可同時使用單數與複數。所有列出的角色都必須存在。`type` 必須與角色 `complex_appraisal` 的 `signal` 完全一致（含大小寫）；`intensity` 為 `0.0～1.0`；`elicitor` 必填。若 Alice 有 `direct_act / Help / performer Bob` 規則，她會以該規則評估；Bob 可套用 `self_act`；Anne 可套用 `indirect_act`。HTTP `accepted` 只表示原生訊號已交給 ALMA；沒有相符 Complex Appraisal 時，本來就可能不產生新情緒。

原始群組模型會自動把「群組成員作為 performer」的 Act、EmotionDisplay、MoodDisplay 送給該群組當 listener；不要求 request 的 addressee/listener 也是成員，也不需要在 request 指定群組。群組是否產生 affect 取決於自己的 matching indirect rule。

### EmotionDisplay 與 MoodDisplay

這兩者是論文與 XSD 定義的獨立原生輸入，不等於 `/event`：

```bash
curl -X POST http://localhost:8080/emotion-display \
  -H 'Content-Type: application/json' \
  -d '{"performer":"Alice","type":"Joy","intensity":0.7,"elicitor":"show-joy-1"}'

curl -X POST http://localhost:8080/mood-display \
  -H 'Content-Type: application/json' \
  -d '{"performer":"Alice","type":"Hostile","intensity":0.7,"elicitor":"show-mood-1"}'
```

以上範例可配合更新後的 `scripts/CreateCharacterExample.json` 執行，因為 Alice 有 `self_emotion / Joy` 與 `self_mood / Hostile` 規則。

EmotionDisplay／MoodDisplay 表示「角色表達、自己與其他角色觀察到的社交訊號」，不是直接把 performer 的 current emotion/mood 設成 `type`。Emotion type 必須是 ALMA EmotionName（例如 `Joy`、`Distress`、`Physical`）；Mood type 必須是 9 個 MoodWord 之一：`Exuberant`、`Bored`、`Dependent`、`Disdainful`、`Relaxed`、`Anxious`、`Docile`、`Hostile`、`Neutral`。參與者欄位規則與 `/act` 相同。

原始核心還有一個重要 gate：performer 必須先有 matching `self_emotion`／`self_mood`，核心才會繼續處理角色 addressee/listener；否則會提前 return。REST 有明列角色收件者而缺 self rule 時回 `422`，避免錯誤回報 accepted。Observer 端則使用 `indirect_emotion`／`indirect_mood`；同一 external rule 同時適用 addressee 與 listener，原始模型沒有 `direct_emotion` 或 `direct_mood` kind。若沒有角色收件者，但 performer 所屬群組有 matching indirect rule，群組路徑仍可單獨運作，回應會以 `group_rule_matched` 明示。

## 16. 查詢角色情感

查詢單一角色：

```bash
curl http://localhost:8080/affect/Alice
```

回應主要欄位：

```json
{
  "name": "Alice",
  "affect_computation_paused": false,
  "personality": {
    "openness": 0.5,
    "conscientiousness": 0.3,
    "extraversion": 0.7,
    "agreeableness": 0.6,
    "neurotism": -0.2,
    "derived": false,
    "emotion_influence": 0.2
  },
  "dominant_emotion": {
    "name": "Joy",
    "intensity": 0.42,
    "baseline": 0.05,
    "active": true,
    "elicitor": "gift-20260814-001",
    "elicited_at": 1786720000000,
    "pad": {
      "pleasure": 0.4,
      "arousal": 0.2,
      "dominance": 0.1
    },
    "appraisal": {
      "desirability": 0.7,
      "praiseworthiness": null,
      "appealingness": null,
      "likelihood": null,
      "realization": null,
      "liking": null,
      "agency": "self"
    }
  },
  "mood": {
    "word": "Exuberant",
    "intensity": "moderate",
    "pleasure": 0.46,
    "arousal": 0.37,
    "dominance": 0.40
  },
  "mood_tendency": {
    "word": "Exuberant",
    "intensity": "moderate",
    "pleasure": 0.47,
    "arousal": 0.38,
    "dominance": 0.41
  },
  "default_mood": {
    "word": "Exuberant",
    "intensity": "moderate",
    "pleasure": 0.46,
    "arousal": 0.37,
    "dominance": 0.40
  },
  "emotions": [
    {
      "name": "Joy",
      "intensity": 0.42,
      "baseline": 0.05,
      "active": true,
      "elicitor": "gift-20260814-001",
      "elicited_at": 1786720000000,
      "pad": {
        "pleasure": 0.4,
        "arousal": 0.2,
        "dominance": 0.1
      },
      "appraisal": {
        "desirability": 0.7,
        "praiseworthiness": null,
        "appealingness": null,
        "likelihood": null,
        "realization": null,
        "liking": null,
        "agency": "self"
      }
    }
  ]
}
```

- `affect_computation_paused`：是否暫停 timer 驅動的 emotion decay 與 mood computation。
- `dominant_emotion`：依最大 `(intensity - baseline)` 選出的情緒，不是單看絕對 intensity；沒有 active emotion 時為 `Undefined`。
- `personality`：Big Five、`derived` metadata 與人格影響情緒的權重。
- `mood`：當前心情詞、分級與完整 PAD 座標。
- `mood_tendency`：ALMA 計算的當前心情趨勢與 PAD。
- `default_mood`：由人格導出的回歸目標心情與 PAD。
- `emotions`：所有可用情緒。`baseline` 是人格 baseline 與 current mood 推導出的 moody baseline 平均值，不一定是固定人格常數；`active` 表示強度是否高於 baseline。
- `elicitor`／`elicited_at`：真正事件情緒的來源與建立時間。純 personality/mood baseline 沒有被事件 elicited，兩欄均為 `null`，不會假造時間。
- `pad`：一般 OCC emotion 使用全域 `MoodRelations/EmotionRelation` 映射；`Physical` 使用本次 PAD 輸入座標。這也是 ALMA MoodEngine 實際採用的方向。
- `appraisal`：核心保存在該 emotion 上的 OCC appraisal variables；沒有 appraisal（例如純 baseline 或 Physical）時為 `null`。`agency` 為 `self`、`other` 或 `null`。

查詢所有角色：

```bash
curl http://localhost:8080/affect
```

## 17. 建立與查詢群組

動態建立群組：

```bash
curl -X POST http://localhost:8080/groups \
  -H 'Content-Type: application/json' \
  -d '{
    "name": "Dynamic Trio",
    "characters": ["Anne", "Bob", "Alice"]
  }'
```

`name` 必須唯一；`characters` 至少兩名、不可重複，而且每個角色必須已存在。動態群組與動態角色相同，只保存在本次程序記憶體。可選填區塊如下：

- `mood`：只含 `decay_time`、`decay_period`，沒有角色 mood 的 `neurotism_stability`。
- `emotion`：只含 `decay_time`、`decay_period`、`decay_function`，沒有角色 emotion 的 `baseline`。
- `appraisal`：完整 18-tag Basic appraisal。
- `complex_appraisal`：group 依原始 XSD 僅支援 `indirect_act`、`indirect_emotion`、`indirect_mood`。

每條 group complex rule 的 `performer` 必須包含在該 group 的 `characters`，否則原始核心永遠不會把訊號路由到這個群組，REST 會回 `400`。若要讓群體本身對成員訊號形成 group-level emotion/mood，必須提供相符的 indirect 規則；省略時使用 adaptor 的 Basic 預設值。原始 XSD 允許 GroupAffect 完全省略 `Appraisal`；REST 動態群組固定建立它，而一旦 `Appraisal` 存在，其中的 Basic 及 18 tags 才是必備結構。啟動設定中省略 group Appraisal 會被 adaptor 忠實視為空規則集。

核心沒有把 `/appraisal`、BasicEEC、Event、Action、Object 直接 target 到 GroupManager 的 runtime 路徑；群組的實際訊號入口是成員的 Act、EmotionDisplay、MoodDisplay。

列出群組：

```bash
curl http://localhost:8080/groups
```

查詢群組 affect（空格須 URL encode）：

```bash
curl 'http://localhost:8080/affect/group/Dynamic%20Trio'
```

回應的 canonical 欄位是原始 XSD `OverallMood` 對應的 `overall_mood`；`meta_mood` 暫留為 deprecated 相容別名，兩者值相同。這是群組抽象 affect unit 自己的 current mood，不是成員 PAD 的簡單平均。回應也包含 `characters` 與 `affect_computation_paused`。

`social_integrity.numeric` 的原始公式是：對每一對成員，加上 `3 × PAD 正負 octant 差異軸數 + PAD 歐氏距離`，最後除以群組成員數。數值越低、完整性越強，故回應以 `lower_is_stronger: true` 明示。`mood_similarities` 是原核心保留的「同一 PAD octant」配對摘要，不要求座標完全相同；核心以 Hashtable 保存，4 人以上時可能覆寫，所以它不是完整的 `C(n,2)` pairwise 統計。`mood_similarity_summary_raw` 保留核心原字串；REST 只把能唯一對回真實成員的項目放入結構化陣列，避免靠 ` - ` 硬拆而虛構角色名稱。`mood_extremes` 實作會列出所有成員，依各自「current mood 到自己的 default mood」距離排序；它沒有 extreme threshold，也不是到 overall mood 的距離。群組統計由 ALMA 隨時間更新。

## 18. 暫停、單步與恢復計算

暫停單一角色的情緒衰減與心情計算：

```bash
curl -X POST 'http://localhost:8080/pause?character=Alice'
```

恢復：

```bash
curl -X POST 'http://localhost:8080/resume?character=Alice'
```

群組可用 `group` 查詢參數單獨控制（空格須 URL encode）：

```bash
curl -X POST 'http://localhost:8080/pause?group=Dynamic%20Trio'
curl -X POST 'http://localhost:8080/resume?group=Dynamic%20Trio'
```

省略 `character`／`group` 會作用於所有角色與所有群組：

```bash
curl -X POST http://localhost:8080/pause
curl -X POST http://localhost:8080/resume
```

回應將「目前狀態」與「本次是否造成轉換」分開，重複 pause 不會再錯報成 resumed：

```json
{"paused":true,"changed":false,"scope":"all"}
```

原始 ALMA 的 pause 定義只停止 timer 驅動的 emotion decay 與 mood computation。`/appraisal`、`/eec` 等同步 inference 仍可加入 character emotion；`/pad` 是核心特例，paused 時會被丟棄，因此 adaptor 直接回 `409 Conflict` 而不謊報 applied。全域 pause 後新建立的角色／群組會繼承 paused 狀態；只有無 query 的全域 `/resume` 會清除這個繼承 latch，單獨 resume 某個 entity 不會改變新 entity 的政策。

原始 group signal path 在 matching member Act/EmotionDisplay/MoodDisplay appraisal 時會自行重開 timer。為了讓 group pause 保持真實且避免 1 ms period 的競態，若該訊號會命中 paused group，REST 會在進核心前回 `409`；請先 resume group 再送訊號。internal appraisal 的群組限制見前節。

paused 狀態可執行原始 stepwise computation：REST 會要求核心額外跑一個 emotion decay pass 與一個 mood computation pass，完成後仍保持 paused。原始 `Timer.cancel()` 無法中止已經進入 callback 的 tick，因此剛 pause 就立刻 step 時屬 tick-boundary best effort，不應拿來當嚴格的 deterministic simulator clock。

```bash
curl -X POST 'http://localhost:8080/step?character=Alice'
curl -X POST 'http://localhost:8080/step?group=Dynamic%20Trio'
curl -X POST http://localhost:8080/step
```

全域 step 要求所有角色與群組都已 pause；指定單一 entity 時只要求該 entity paused。未 pause 會回 `409`。

這三個控制 endpoint 只接受「完全不帶 query」（明確代表全域），或只帶一個 `character=name`／`group=name`。未知、重複、空值或錯誤 URL encoding 一律回 `400`；`/pause/Alice` 之類的額外路徑回 `404`，不會因拼字錯誤意外擴大成全域操作。其他固定 endpoint 也採 exact-path 檢查。

## 19. 建議的應用程式流程

```text
啟動 ALMA REST Server
        ↓
POST /characters 建立執行期角色
        ↓
GET /characters 確認建立成功
        ↓
POST /appraisal、/eec、/act、/emotion-display、/mood-display 或 /pad 傳入外部情境
        ↓
GET /affect/{name} 取得情緒與心情
        ↓
選擇性 POST /groups 並查詢群體 affect
        ↓
遊戲、LLM 或其他服務依情感狀態調整行為
```

若角色必須跨重啟保留，呼叫端應保存建立角色所使用的 JSON，並在 ALMA 每次啟動後重新呼叫 `POST /characters`。目前 API 不會自動持久化角色。
